export type DataFormat = {
  id: number;
  item: string;
  purchaseLinkEurope?: string;
  purchaseLinkUK?: string;
  store: string;
  isSoldOut?: boolean;
  mainColor: string;
  secondColor: string;
  coverImage: string;
};
